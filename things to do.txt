
- Tabelas?

- Collect more data (until august/2021), forcing the composition to be the same

- Do more simulations (|S| = 100) when performing the optimization using GAN's synthetic data 

- Fazer um GA que agregue tanto dados de simulação quanto dados históricos.

min [alpha*TE_hist + (1-alpha) max(TE_simulated)]
min [alpha*TE_hist + (1-alpha) mean(TE_simulated)]